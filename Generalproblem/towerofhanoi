#include <stdio.h>
int toh(int n){
   
    if(n==1){
        return 1;
    }
   return 2*toh(n-1)+1;
}
int main(){
 
  printf("Enter a number for which u wannna check \n");
  int n;
  scanf("%d",&n);
  int y=toh(n);
  printf("No. of attempt reqired %d",y);



}
